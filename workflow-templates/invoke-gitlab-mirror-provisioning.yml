name: Invoke GitLab Mirror Provisioning

on:
  workflow_dispatch:

jobs:
  mirror:
    uses: ${{ github.repository_owner }}/.github/.github/workflows/provision-gitlab-mirror.yml@$default-branch
    secrets: inherit
